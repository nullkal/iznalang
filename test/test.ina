test_img = LoadPNG("test.png")

x = 96
y = 96
MainLoop(\() do
  if KeyIsHolded("UP")
    y = y - 8
  end
  if KeyIsHolded("DOWN")
    y = y + 8
  end
  if KeyIsHolded("LEFT")
    x = x - 8
  end
  if KeyIsHolded("RIGHT")
    x = x + 8
  end

  Draw(test_img, x, y)
end)
