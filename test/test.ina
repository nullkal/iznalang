test_img = LoadPNG("test.png")

count = 0
MainLoop(\() do
  x = 400 + cos(1.0 * count / 600 * 6.28) * 300 - 64
  y = 300 + sin(1.0 * count / 600 * 6.28) * 200 - 57
  Draw(test_img, x, y)

  count = count + 1
end)
